export function RandomCoins() {
  const randomCoins = Math.ceil(Math.random() * 10000);
  return randomCoins;
}

export function NewValue() {
  const newValue = Math.ceil(Math.random() * 5000);
  return newValue;
}
